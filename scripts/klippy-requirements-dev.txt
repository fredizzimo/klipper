# This file describes the Python virtualenv package requirements for
# developing of the Klipper host software (Klippy). These package
# requirements are typically installed via the command:
#   pip install -r klippy-requirements-dev.txt
matplotlib==2.2.4
numpy==1.16.4
pytest==4.6.5
plotly==4.1.0
future==0.17.1